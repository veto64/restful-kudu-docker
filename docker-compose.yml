version: '3.3'
services:
  api:
    image: veto64/restful-kudu-docker
    container_name: restful_api_docker
    volumes: 
     - "./api:/opt/api/"
    ports:
     - 80:80
    networks:
      - queen
networks:
  queen:
